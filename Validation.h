#pragma once


bool isValidCommand(char);

bool isSizeInTheAllowedInterval(int, int, int);

bool isFieldFull(int**, size_t);

bool areThereAnyPossibleMoves(int**, size_t);

bool isFieldFullWithoutCombinations(int**, size_t);
